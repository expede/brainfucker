machine:
  ghc:
    version: 7.8.3
dependencies:
  override:
    - cabal install --only-dependencies
    - cabal configure --enable-tests
    - cabal build
    - cabal test
# test:
#   override:
#     - cabal test:
#         timeout: 300